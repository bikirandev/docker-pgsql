# Docker PostgreSQL Setup

A secure, containerized PostgreSQL setup using Docker Compose for local database management.

## Features

- 🔒 Secure configuration with localhost-only binding
- 📁 Persistent data storage using Docker volumes
- ⚙️ Environment-based configuration
- 🔄 Auto-restart on failure
- 💚 Health checks to monitor database status

## Prerequisites

- Docker
- Docker Compose

## Quick Start

1. **Clone the repository**

   ```bash
   git clone https://github.com/bikirandev/docker-pgsql. git
   cd docker-pgsql